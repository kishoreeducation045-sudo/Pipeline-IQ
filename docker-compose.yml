services:
  backend:
    build: ./backend
    container_name: pipelineiq-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./data:/data
    restart: unless-stopped
